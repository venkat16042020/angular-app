import { Directives1 } from './directives1';

describe('Directives1', () => {
  it('should create an instance', () => {
    const directive = new Directives1();
    expect(directive).toBeTruthy();
  });
});
